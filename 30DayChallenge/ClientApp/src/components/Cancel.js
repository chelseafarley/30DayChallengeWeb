import React, { Component } from 'react';

export class Cancel extends Component {
  static displayName = Cancel.name;

  render () {
    return (
      <div>
        It's a shame you can't support us this time as your payment was unsuccessful. We look forward to your support in the future.
      </div>
    );
  }
}
